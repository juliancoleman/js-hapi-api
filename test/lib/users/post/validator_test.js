const Joi = require("joi");
const R = require("ramda");

const Validator = appRequire("lib/users/post/validator");

describe("PostUsersValidator", () => {
  context("Test not written", () => {
    it("but still passes", async () => {
      expect(true).to.equal(true);
    });
  });
});
