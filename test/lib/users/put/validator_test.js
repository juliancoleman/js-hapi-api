const Joi = require("joi");
const R = require("ramda");

const Validator = appRequire("lib/users/put/validator");

describe("PutUsersValidator", () => {
  context("Test not written", () => {
    it("but still passes", async () => {
      expect(true).to.equal(true);
    });
  });
});
