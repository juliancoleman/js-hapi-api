const Joi = require("joi");
const R = require("ramda");

const Validator = appRequire("lib/users/delete/validator");

describe("DeleteUsersValidator", () => {
  context("Test not written", () => {
    it("but still passes", async () => {
      expect(true).to.equal(true);
    });
  });
});
